# 📘 Git Ignore (.gitignore) – Important Notes

---

## 🔹 Git Ignore kya hota hai?
- **.gitignore** ek special file hoti hai  
- Is file ke andar hum Git ko batate hain:
  - kaun-si files  
  - kaun-si folders  
  Git ko **track nahi karni chahiye**

👉 Matlab:
> “Ye files mere project ka part hain,  
> lekin GitHub par push nahi honi chahiye.”

---

## 🔹 Git Ignore kyu important hai?
- Har file ko GitHub par push karna **safe ya useful nahi hota**
- Kuch files:
  - bahut heavy hoti hain  
  - sensitive hoti hain (passwords, API keys)  
- Interviews + real projects dono me **bahut important concept** hai

---

## 🔹 Git Ignore kab use hota hai?
Jab aap:
- code commit karte ho  
- code GitHub par push karte ho  

Aur chahte ho ki:
- kuch files/folders **ignore** ho jaayein  

---

## 🔹 Common examples jo ignore ki jaati hain
- `node_modules/` (React, Angular, Next.js projects)
- `.env` files (API keys, DB passwords)
- Log files
- Build files
- OS generated files

---

## 🔹 Project setup (recap)
1. Working Directory banayi
2. VS Code / kisi IDE me open ki
3. Files create ki:
   - `index.html`
   - `app.js`
   - `style.css`
   - `passwords/` (folder)

4. Git initialize ki:
```bash
git init
🔹 .gitignore file kya hoti hai?
Project ki root directory me hoti hai

Isme likha hota hai:

kaun-si file

kaun-si directory
Git ko ignore karni hai

📌 .gitignore khud Git me track hoti hai

🔹 File extension ke basis par ignore karna
Agar aap chahte ho:

sabhi .css files ignore ho jaayein

👉 .gitignore me likho:

*.css
📌 * ka matlab:

file ka naam kuch bhi ho

agar extension .css hai → ignore

🔹 Specific file ignore karna
Agar sirf ek hi file ignore karni ho
(example: app.js)

👉 .gitignore me likho:

app.js
🔹 Directory (folder) ignore karna
Agar poora folder ignore karna ho
(example: passwords)

👉 .gitignore me likho:

passwords/
📌 Is folder ke andar:

1 file ho ya

1000 files ho
sab ignore ho jaayengi

🔹 Multiple rules ek saath
Example .gitignore file:

*.css
app.js
passwords/
.env
node_modules/
🔹 git status (very important command)
Ye command batati hai:

kaun-si file modified hai

kaun-si untracked hai

kaun-si stage me hai

👉 Command:

git status
🔹 .gitignore ka effect
Jo file .gitignore me likhi hoti hai:

wo stage nahi hoti

commit nahi hoti

GitHub par push nahi hoti

⚠️ Note:

Agar file pehle se commit ho chuki hai,
to .gitignore us par apply nahi hota

🔹 Git Ignore ke baad workflow
.gitignore file create / update

Check status:

git status
Stage files:

git add .
Commit:

git commit -m "git ignore file added"
Push:

git push
🔹 GitHub par kya push hota hai?
✅ index.html

✅ .gitignore

❌ style.css (ignored)

❌ app.js (ignored)

❌ passwords/ folder (ignored)

🔹 Interview ke liye One-Line Summary
.gitignore Git ko batata hai
kaun-si files/folders track nahi karne hain

Security + performance ke liye use hota hai

*.ext → extension ignore

folder/ → directory ignore

.env, node_modules sabse common examples

🔹 Very Important Interview Tip ⭐
.gitignore file GitHub par push hoti hai,
lekin uske andar likhi files push nahi hoti
